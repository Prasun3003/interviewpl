{
  "name": "interviewpl",
  "version": "1.0.0",
  "description": "",
  "main": "backend/src/server.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start": "node backend/src/server.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "vercel-build": "npm install && cd frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Prasun3003/interviewpl.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "inngest": "^3.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "bugs": {
    "url": "https://github.com/Prasun3003/interviewpl/issues"
  },
  "homepage": "https://github.com/Prasun3003/interviewpl#readme"
}
